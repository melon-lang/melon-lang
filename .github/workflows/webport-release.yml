name: "tagged-release"

permissions: write-all

on:
  push:
    branches: [ "main" ]

jobs:
  tagged-release:
    name: "Tagged Release"
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: 'npm'
      - run: npm ci
      - run: npm run build:webport
      - run: mv dist/webport/index.html dist/webport/build

      - name: "call action"
        id: last_release
        uses: InsonusK/get-latest-release@v1.0.1
        with:
          myToken: "${{ secrets.GITHUB_TOKEN }}"
          exclude_types: "release"
          view_top: 1
      
      - name: Bump release version
        id: bump_version
        uses: christian-draeger/increment-semantic-version@1.1.0
        with:
          current-version: "${{ vars.VERSION }}" # also accepted: 'v2.11.7-alpha.3' | '2.11.7-alpha3'
          version-fragment: 'bug'
      
      - name: Do something with your bumped release version
        run: echo ${{ steps.bump_version.outputs.next-version }}
        # will print 2.12.0
      
      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          title: "Webport Build"
          automatic_release_tag: "${{ steps.bump_version.outputs.next-version }}"
          prerelease: true
          files: |
            dist/webport/build
